# V6.3 — summon → pixels, end to end

**Phase:** 6   **Needs:** a real Mac with a window server (an agent/CI host has none). No human
interaction beyond launching it.
**Discharges:** the Phase 0 target "summon → pixels on screen < 100 ms", which was budget-allocated
(drawing 1.3 ms D13, capture ~46 ms and unparallelisable D12, enumeration 0.30 ms warm D5) but
never timed end to end on the assembled app.

## Goal

Measure the wall-clock from "summon gesture recognised" to "the panel's first frame is on screen",
against the **< 100 ms** budget. Timed to the **CA commit**, not to the call returning — D13 showed
`orderFrontRegardless` returns in ~1 ms while the cold frame does not commit for ~14 ms, so timing
the call is wrong by 10x.

## Prerequisites

- `build/GoTab.app`, Accessibility granted (Screen Recording optional — thumbnails are prefetched
  off the summon path by design, D12, so their absence must not change the number).
- Instrumentation: add a timestamp at gesture recognition (`hotkey.m`'s gesture post) and one in a
  `CATransaction` completion handler on the panel's first show (`panel.m`), printed to stderr. This
  is V6.3's own scaffold — keep it behind a build tag or a `-timing` flag, do not ship it.

## Protocol

1. Launch the bundle. Let it settle (the first enumeration's cold cost is paid at launch, D5 — it is
   not part of a summon).
2. Summon 30 times with the panel fully dismissed between each. Record gesture→commit for each.
3. Report cold (first summon after launch) separately from warm (the rest), min / median / p95 /
   max.

## Acceptance

- [x] Warm p95 gesture → CA-commit **< 100 ms** — **15.25 ms** (min 1.83, median 8.43, max 19.89),
      29 warm samples, 2026-09-08. **7x under budget.** D50.
- [x] Cold summon reported separately — **74.24 ms** (first summon after launch), inside budget;
      framework/first-frame warm-up, not a fail (D5).
- [ ] The number holds with Screen Recording **off** — **not yet run.** SR was granted for D50's
      run. The warm median of 8 ms already implies capture is off the summon path (a captured
      thumbnail is ~46 ms, D12; the prefetcher does it ahead of time), but the clean control run
      is still owed. Re-run: `tccutil reset ScreenCapture app.gotab`, then `gotab -timing`.
- [x] Numbers appended to `docs/DECISIONS.md` (D50); `docs/ROADMAP.md` Phase 0 target table
      ("summon → pixels") and Phase 6 V6.3 row updated.

## The scaffold

`gotab -timing [-timing-n N]` (default 30) — `cmd/gotab` `runTiming` / `timingDriver` /
`reportTiming`, plus `internal/platform/darwin/timing.{h,go}` and a one-shot `CATransaction`
completion block in `panel.m`'s `gt_panel_show` armed only by `gt_timing_arm()`. It runs the whole
switcher render pipeline (CreatePanel, event loop, prefetcher, renderer) minus the hotkey and the
permission gate, and drives N summons via `Loop.Post`, timing each from the post to the frame
reaching the render server (the same edge `spike/panel` measured, D13). It is inert in the shipped
switcher — `g_timing_armed` starts 0 and `gotab -switch` never arms it. Delete with V6.3 once the
SR-off control is also done, or keep if it earns its place as a perf regression check.

## Findings — D50 (2026-09-08)

- Host macOS 26.6.2, both grants, **only 2–3 windows open** during the run (GoLand ×1–2, Chrome ×1).
  The draw path scales gently with tile count (D13), but a 30–50-window run would be a firmer test;
  recorded as a caveat, not a blocker — median 8 ms leaves a very wide margin.
- Headless-driven (`Loop.Post`), which V6.3 sanctions ("no human interaction beyond launching it").
  `t0` is the programmatic post, not a HID ⌥⇥ — hotkey delivery is V6.1's number (3.2 ms worst case,
  D45). End to end, ⌥⇥ → pixels ≈ 3.2 + 15.25 ≈ **~18.5 ms p95**, ~18 % of the 100 ms budget.
- 30/30 summons committed; no `CATransaction` completion ever timed out.

## If warm p95 is over 100 ms

The budget breakdown (D12/D13/D5) says the spend is on data. Look first at whether `doRescan` is
running on the summon path (it must not — enumeration is event-driven, the summon only reconciles
selection and lays out), then at `panelRenderer.onState` doing work it could have done ahead of time.
Send the finding back to the phase that owns the cost.
